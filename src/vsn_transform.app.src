{application, vsn_transform, [
    {description, "Apply git vsn to modules"},
    {vsn, git},
    {registered, []},

    {applications, [
        % OTP dependencies
        kernel,
        stdlib
        % External dependencies
    ]},

    {env, []}
]}.
